#ifndef __DEFINES_HPP__
#define __DEFINES_HPP__

#define _DEBUG
#define _ELPP_DISABLE_DEFAULT_CRASH_HANDLING

#define PTM_RATIO 50.f
#define PTOM(p) (p / PTM_RATIO)
#define MTOP(m) (m * PTM_RATIO)

#endif
